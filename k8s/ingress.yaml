apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mbadet-vitrine
  namespace: mbadet-vitrine
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - mbadet.fr
        - www.mbadet.fr
      secretName: mbadet-vitrine-tls
  rules:
    - host: mbadet.fr
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mbadet-vitrine
                port:
                  number: 80
    - host: www.mbadet.fr
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mbadet-vitrine
                port:
                  number: 80
